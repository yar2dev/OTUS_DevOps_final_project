---
apiVersion: v1
kind: Service
metadata:
  name: crawler
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8000
      nodePort: 31800
  selector:
    app: search_engine
    component: crawler
  type: LoadBalancer

---
apiVersion: v1
kind: Service
metadata:
  name: crawler
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: search_engine
    component: crawler
  type: ClusterIP